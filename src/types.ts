export interface Product {
  name: string;
  brand: string;
  category: string;
  description: string;
  website: string;
}

export interface Store {
  name: string;
  category: string;
  description: string;
  website: string;
}

export interface Company {
  name: string;
  industry: string;
  description: string;
  website: string;
}

export interface YouTuber {
  name: string;
  category: string;
  description: string;
  channel: string;
}
